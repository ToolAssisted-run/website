# drops into /etc/nginx/conf.d/: Ubuntu's nginx.conf says `gzip on` but
# compresses only text/html by default; the site's other text deserves it too
gzip_types text/css application/javascript application/json image/svg+xml
           text/plain application/xml;
